import { ShiftHandoverModels } from './shift-handover-models';

describe('ShiftHandoverModels', () => {
  it('should create an instance', () => {
    expect(new ShiftHandoverModels()).toBeTruthy();
  });
});
